{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>User index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Password</th>
                <th>IsVerified</th>
                <th>Username</th>
                <th>SmsSend</th>
                <th>RegisteredDate</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                <td>{{ user.password }}</td>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
                <td>{{ user.username }}</td>
                <td>{{ user.smsSend }}</td>
                <td>{{ user.registeredDate ? user.registeredDate|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_edit_profile_show', {'id': user.id}) }}">show</a>
                    <a href="{{ path('app_edit_profile_edit', {'id': user.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_edit_profile_new') }}">Create new</a>
{% endblock %}
